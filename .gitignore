# Claude Code local settings (may contain sensitive data)
.claude/settings.local.json

# ChromaDB local data
chroma-data/
chroma-data.bak/

# Local TODO
TODO.md

# Demo screenshots captured by the rosetta-demo-capture skill
demo-screenshots/

# Playwright build artefacts (Playwright project lives inside the
# rosetta-add-framework-playwright skill; only node_modules and test
# outputs are ignored)
.claude/skills/rosetta-add-framework-playwright/test-results/
.claude/skills/rosetta-add-framework-playwright/playwright-report/
.claude/skills/rosetta-add-framework-playwright/blob-report/
.claude/skills/rosetta-add-framework-playwright/.cache/

# Python virtual environment (for ChromaDB server)
.venv/

# Python cache
__pycache__/
.DS_Store

# dependencies
node_modules/

.env
.env.local
.env.test-local

# Externally installed Arize / Phoenix skills (synced from skills-lock.json)
.agents/skills/arize-*
.agents/skills/phoenix-*
.claude/skills/arize-*
.claude/skills/phoenix-*
skills-lock.json
